import {projects} from './portfolio-data.mjs';
const escape=value=>String(value).replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll('<','&lt;').replaceAll('>','&gt;');
export function portfolioCarousel(id){
 const items=projects.filter(p=>p.type==='concept'||p.publicationApproved===true);
 return `<section class="portfolio-carousel" data-carousel role="region" aria-roledescription="carrousel" aria-label="Interactieve websiteconcepten"><div class="carousel-media">${items.map((p,i)=>`<a data-carousel-image="${i}" ${i?'hidden':''} href="${escape(p.url)}" target="_blank" rel="noopener noreferrer" aria-label="${escape(p.title)} — open live demo in nieuw tabblad"><img src="${escape(p.image.src)}" alt="${escape(p.image.alt)}" width="${p.image.width}" height="${p.image.height}" loading="lazy" decoding="async"></a>`).join('')}</div><div class="carousel-controls" data-carousel-controls hidden><button type="button" data-carousel-prev aria-label="Vorig project">←</button><div class="carousel-pagination" aria-label="Kies een project">${items.map((p,i)=>`<button type="button" data-carousel-to="${i}" aria-label="Toon ${escape(p.title)}" aria-current="${i?'false':'true'}" aria-controls="${id}-${p.id}"><span aria-hidden="true"></span></button>`).join('')}</div><button type="button" data-carousel-next aria-label="Volgend project">→</button></div><div class="carousel-details">${items.map((p,i)=>`<article id="${id}-${p.id}" data-carousel-slide="${i}" role="group" aria-roledescription="dia" aria-label="${i+1} van ${items.length}: ${escape(p.title)}" ${i?'hidden':''}><div class="carousel-meta"><span class="carousel-badge">Interactieve demo</span><span>Websiteconcept</span></div><p class="carousel-industry">${escape(p.industry)}</p><h3>${escape(p.title)}</h3><p class="carousel-description">${escape(p.description)}</p><a class="button primary carousel-cta" href="${escape(p.url)}" target="_blank" rel="noopener noreferrer">Bekijk de live demo <span aria-hidden="true">↗</span></a></article>`).join('')}</div><p class="sr-only" data-carousel-status role="status" aria-live="polite" aria-atomic="true"></p><noscript><p class="carousel-noscript">Meer demo’s: ${items.map(p=>`<a href="${escape(p.url)}" target="_blank" rel="noopener noreferrer">${escape(p.title)}</a>`).join(' · ')}</p></noscript></section>`;
}
